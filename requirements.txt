# Python dependencies for the sRNA RNA-Seq / co-expression pipeline GUI
# Install with:  pip install -r requirements.txt
# (the setup scripts do this for you inside a local .venv)

PySide6>=6.5      # GUI framework (app.py)
pandas>=1.5       # data wrangling (pipeline_logic.py)
numpy>=1.23       # numerics
openpyxl>=3.0     # Excel (.xlsx) writing for pandas .to_excel()

# Note: R is a separate runtime, not a pip package.
# Run install_packages.R (or the setup script) to install the R/Bioconductor deps:
#   WGCNA, DESeq2, tibble, fastDummies, flashClust
